<div class="row">
	<div class="showplant">
		<a href="/plants" class="green lighten-1 waves-effect waves-light btn-large">See All Vegetables</a>
		<h1><%= months.name %></h1>
	</div>	
</div>
<%= months.name %>

<div class="centerit">
	<% foo.forEach(function(foo) { %>
		<%= foo.name %>
	<% }) %>


	<% bar.forEach(function(bar) { %>
		<%= bar.name %>
	<% }) %>
</div>	